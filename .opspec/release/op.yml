description: Runs all ops required to release
name: release
inputs:
  - { name: AWS_ACCESS_KEY_ID, description: Access key for AWS, isSecret: true }
  - { name: AWS_SECRET_ACCESS_KEY, description: Secret access key for AWS, isSecret: true}
run:
  serial:
  - op: build
  - op: deploy-to-s3
